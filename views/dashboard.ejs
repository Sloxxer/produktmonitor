<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Bevakningar</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body class="min-h-screen bg-gray-100">
    <div class="max-w-3xl mx-auto py-8">
      <div class="flex justify-between items-center mb-6">
        <h1 class="text-3xl font-bold">Hej <%= user.username %>!</h1>
        <form action="/logout" method="POST"><button class="btn-primary">Logga ut</button></form>
      </div>

      <form class="mb-8 flex gap-2" action="/products" method="POST">
        <input name="url" class="flex-1 input" placeholder="Produkt‑URL …" required />
        <button class="btn-primary">Lägg till</button>
      </form>

      <div class="grid gap-4">
        <% if (products.length === 0) { %>
          <p class="text-gray-600">Du följer inga produkter ännu.</p>
        <% } else { %>
          <% products.forEach(p => { %>
            <div class="card">
              <a class="link" href="<%= p.url %>" target="_blank"><%= p.url %></a>
              <form action="/products/<%= p.id %>/delete" method="POST">
                <button class="text-red-600 hover:text-red-800">🗑</button>
              </form>
            </div>
          <% }) %>
        <% } %>
      </div>
    </div>
  </body>
</html>