<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Bevakningar</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <div class="container">
      <div class="mb-6" style="display:flex;justify-content:space-between;align-items:center;">
        <h1 style="font-size:1.75rem;font-weight:700;">Hej <%= user.username %>!</h1>
        <form action="/logout" method="POST"><button class="btn-primary">Logga ut</button></form>
      </div>

      <form class="mb-8" style="display:flex;gap:.5rem;" action="/products" method="POST">
        <input name="url" class="input" placeholder="Produkt‑URL …" required />
        <button class="btn-primary">Lägg till</button>
      </form>

      <div class="grid-gap">
        <% if (products.length === 0) { %>
          <p class="text-sm">Du följer inga produkter ännu.</p>
        <% } else { %>
          <% products.forEach(p => { %>
            <div class="card">
              <a class="link" href="<%= p.url %>" target="_blank"><%= p.url %></a>
              <form action="/products/<%= p.id %>/delete" method="POST">
                <button style="color:#e11d48;font-size:1.2rem;background:none;border:none;cursor:pointer;">🗑</button>
              </form>
            </div>
          <% }) %>
        <% } %>
      </div>
    </div>
  </body>
</html>